using Godot;
using System;

public class InventorySlotDisplay : CenterContainer
{
	/*
	public void DisplayItem(Item item){
		//display item texture in the inventory slot
		//if empty slot, load an empty slot texture
	}
	
	//the following functions may be built into Godot already
	
	public void getDragData(int position){
		var itemIndex = getIndex();
		var item = inventory.RemoveItem(itemIndex);
		
		//if item is Item{
		//var data = {}
		//data.item = item
		//data.itemIndex = itemIndex
		//var dragPreview = TextureRect.New()
		//dragPreview.Texture = item.Texture
		//setDragPreview(dragPreview)
		//return data
		//}
	}
	
	public void canDropData(int position, data){
		//return data is Dictionary and data.Has("item")
	}
	
	public void dropData(int position, data){
		var itemIndex = getIndex();
		var item = item = inventory.Items(itemIndex);
		
		//inventory.swapItems(itemIndex, data.itemIndex)
		//inventory.SetItem(itemIndex, data.Item)
	}
	*/
}
